apiVersion: 1

# Alert rules configured manually via Grafana UI (Phase 7)
# Automation via provisioning requires specific datasource UID setup.
# See RUNBOOK.md §AlertSetup for manual setup instructions.
groups: []
